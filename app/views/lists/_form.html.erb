<%= form_for [current_user, @list] do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :'Nombre de lista' %><br>
    <%= f.text_field :name, class: 'form-control' %>
    <p class="help-block">Una lista incluirá todas las comunas con las que se trabajan y los costos de tranporte</p>
  </div>
  <div class="actions" class="btn btn-primary">
    <%= f.submit "Guardar lista"%>
   </div>
<% end %>
